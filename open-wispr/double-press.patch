diff --git a/Sources/OpenWisprLib/AppDelegate.swift b/Sources/OpenWisprLib/AppDelegate.swift
index 33b809e..cc9079e 100644
--- a/Sources/OpenWisprLib/AppDelegate.swift
+++ b/Sources/OpenWisprLib/AppDelegate.swift
@@ -134,7 +134,8 @@ public class AppDelegate: NSObject, NSApplicationDelegate {
         for hk in config.hotkeys {
             let manager = HotkeyManager(
                 keyCode: hk.keyCode,
-                modifiers: hk.modifierFlags
+                modifiers: hk.modifierFlags,
+                doublePress: hk.doublePress ?? false
             )
             manager.start(
                 onKeyDown: { [weak self] in
@@ -142,6 +143,12 @@ public class AppDelegate: NSObject, NSApplicationDelegate {
                 },
                 onKeyUp: { [weak self] in
                     self?.handleKeyUp()
+                },
+                onDoublePress: { [weak self] in
+                    self?.handleDoublePress()
+                },
+                onSinglePressDown: { [weak self] in
+                    self?.handleSinglePressStop()
                 }
             )
             hotkeyManagers.append(manager)
@@ -196,11 +203,14 @@ public class AppDelegate: NSObject, NSApplicationDelegate {
         for hk in config.hotkeys {
             let manager = HotkeyManager(
                 keyCode: hk.keyCode,
-                modifiers: hk.modifierFlags
+                modifiers: hk.modifierFlags,
+                doublePress: hk.doublePress ?? false
             )
             manager.start(
                 onKeyDown: { [weak self] in self?.handleKeyDown() },
-                onKeyUp: { [weak self] in self?.handleKeyUp() }
+                onKeyUp: { [weak self] in self?.handleKeyUp() },
+                onDoublePress: { [weak self] in self?.handleDoublePress() },
+                onSinglePressDown: { [weak self] in self?.handleSinglePressStop() }
             )
             hotkeyManagers.append(manager)
         }
@@ -271,6 +281,36 @@ public class AppDelegate: NSObject, NSApplicationDelegate {
         }
     }
 
+    private func handleDoublePress() {
+        guard isReady else { return }
+
+        switch recordingLifecycle.keyDown(toggleMode: true) {
+        case .startRecording:
+            print("Double-press: start recording")
+            handleRecordingStart()
+        case .stopRecording:
+            print("Double-press: stop recording")
+            handleRecordingStop()
+        default:
+            break
+        }
+    }
+
+    private func handleSinglePressStop() {
+        guard isReady else { return }
+
+        if recordingLifecycle.isRecording {
+            print("Single-press: stop recording")
+            handleRecordingStop()
+        }
+    }
+
+    private func setHotkeyMode(_ mode: HotkeyPressMode) {
+        for m in hotkeyManagers {
+            m.mode = mode
+        }
+    }
+
     private func handleRecordingStart() {
         statusBar.state = .recording
         do {
@@ -282,15 +322,18 @@ public class AppDelegate: NSObject, NSApplicationDelegate {
             }
             try recorder.startRecording(to: outputURL)
             currentRecordingURL = outputURL
+            setHotkeyMode(.singlePress)
         } catch {
             print("Error: \(error.localizedDescription)")
             recordingLifecycle.recordingStartFailed()
             currentRecordingURL = nil
             statusBar.state = .idle
+            setHotkeyMode(.doublePress)
         }
     }
 
     private func handleRecordingStop() {
+        setHotkeyMode(.doublePress)
         guard let audioURL = recorder.stopRecording() else {
             RecordingCancellation.discardTrackedPartialRecording(&currentRecordingURL)
             statusBar.state = .idle
diff --git a/Sources/OpenWisprLib/Config.swift b/Sources/OpenWisprLib/Config.swift
index 479ffee..1ed1342 100644
--- a/Sources/OpenWisprLib/Config.swift
+++ b/Sources/OpenWisprLib/Config.swift
@@ -332,10 +332,12 @@ public struct FlexBool: Codable {
 public struct HotkeyConfig: Codable, Equatable {
     public var keyCode: UInt16
     public var modifiers: [String]
+    public var doublePress: Bool?
 
-    public init(keyCode: UInt16, modifiers: [String]) {
+    public init(keyCode: UInt16, modifiers: [String], doublePress: Bool? = nil) {
         self.keyCode = keyCode
         self.modifiers = modifiers
+        self.doublePress = doublePress
     }
 
     public var modifierFlags: UInt64 {
diff --git a/Sources/OpenWisprLib/HotkeyManager.swift b/Sources/OpenWisprLib/HotkeyManager.swift
index 1e31b3a..0f63e7e 100644
--- a/Sources/OpenWisprLib/HotkeyManager.swift
+++ b/Sources/OpenWisprLib/HotkeyManager.swift
@@ -1,22 +1,75 @@
 import AppKit
 import Foundation
 
+enum HotkeyPressMode {
+    /// Idle: only a double press fires (single presses are ignored).
+    case doublePress
+    /// Recording: a single, blank press fires (to stop recording).
+    case singlePress
+}
+
 class HotkeyManager {
     private var globalMonitor: Any?
     private let keyCode: UInt16
     private let requiredModifiers: UInt64
+    private let doublePressEnabled: Bool
+
+    var mode: HotkeyPressMode = .doublePress {
+        didSet {
+            if mode != oldValue {
+                modeChangedAt = ProcessInfo.processInfo.systemUptime
+                startPending = false
+                stopPending = false
+                pendingStartCancelled = false
+                pendingStopCancelled = false
+            }
+        }
+    }
+
     private var onKeyDown: (() -> Void)?
     private var onKeyUp: (() -> Void)?
+    private var onDoublePress: (() -> Void)?
+    private var onSinglePressDown: (() -> Void)?
     private var modifierPressed = false
 
-    init(keyCode: UInt16, modifiers: UInt64 = 0) {
+    // Tap history for this key (uptime seconds of real down/up events).
+    private var lastDownAt: TimeInterval = 0
+    private var lastUpAt: TimeInterval = 0
+    private var modeChangedAt: TimeInterval = 0
+
+    // --- double-press (start) detection ---
+    // Two taps = down, up, down again. The window is measured from the first
+    // RELEASE (up -> down gap <= dpGap), not from the first press, so
+    // deliberate taps whose press-to-press span exceeds 400 ms still register.
+    private let dpGap: TimeInterval = 0.4            // release -> second press limit
+    private let dpNoUpFallback: TimeInterval = 0.15  // second down this close to the first down fires even if the OS never delivered the up (merged ultra-fast taps)
+    private var startPending = false                 // second tap seen; awaiting chord-confirm grace
+    private var pendingStartCancelled = false
+
+    // --- single-press stop (blank press) detection ---
+    // Recording stops only on a BLANK press: ctrl down, no other key between
+    // down and up. Chords (ctrl+C, ctrl+A, ctrl+shift+C, ...) never stop it.
+    // The stop fires after a short post-release grace so chord keys delivered
+    // slightly late still cancel it.
+    private let stopGrace: TimeInterval = 0.09       // post-release chord-confirm window
+    private let quietGap: TimeInterval = 0.3         // ignore presses right after a mode change (buzz tails)
+    private var stopPending = false                  // armed on ctrl down while recording
+    private var pendingStopCancelled = false
+
+    init(keyCode: UInt16, modifiers: UInt64 = 0, doublePress: Bool = false) {
         self.keyCode = keyCode
         self.requiredModifiers = modifiers
+        self.doublePressEnabled = doublePress
     }
 
-    func start(onKeyDown: @escaping () -> Void, onKeyUp: @escaping () -> Void) {
+    func start(onKeyDown: @escaping () -> Void,
+               onKeyUp: @escaping () -> Void,
+               onDoublePress: (() -> Void)? = nil,
+               onSinglePressDown: (() -> Void)? = nil) {
         self.onKeyDown = onKeyDown
         self.onKeyUp = onKeyUp
+        self.onDoublePress = onDoublePress
+        self.onSinglePressDown = onSinglePressDown
 
         let mask: NSEvent.EventTypeMask = [.keyDown, .keyUp, .flagsChanged]
 
@@ -33,6 +86,15 @@ class HotkeyManager {
     }
 
     private func handleEvent(_ event: NSEvent) {
+        if doublePressEnabled && isModifierOnlyKey(keyCode) {
+            if mode == .singlePress {
+                handleSinglePressEvent(event)
+            } else {
+                handleDoublePressEvent(event)
+            }
+            return
+        }
+
         if isModifierOnlyKey(keyCode) {
             guard event.type == .flagsChanged else { return }
             guard event.keyCode == keyCode else { return }
@@ -62,6 +124,114 @@ class HotkeyManager {
         }
     }
 
+    // MARK: - Recording: stop on a BLANK press only (never on chords)
+
+    private func handleSinglePressEvent(_ event: NSEvent) {
+        guard event.type == .flagsChanged, event.keyCode == keyCode else {
+            // Any other key activity while a stop press is pending means this
+            // is a chord (ctrl+C / ctrl+shift+C / ...): cancel the stop.
+            if stopPending {
+                stopPending = false
+                pendingStopCancelled = true
+            }
+            return
+        }
+
+        let now = ProcessInfo.processInfo.systemUptime
+        let isDown = !modifierPressed
+
+        if isDown {
+            modifierPressed = true
+            lastDownAt = now
+
+            // Multi-tap buzz tails right after the recording started must not
+            // stop the recording that just began: require quiet since the last
+            // release AND since the mode change.
+            let quietSinceUp = lastUpAt > 0 ? now - lastUpAt : .infinity
+            let quietSinceMode = now - modeChangedAt
+            guard quietSinceUp >= quietGap || quietSinceMode >= quietGap else {
+                return
+            }
+
+            stopPending = true
+            pendingStopCancelled = false
+        } else {
+            modifierPressed = false
+            lastUpAt = now
+
+            if stopPending {
+                // Blank press. Confirm after a short grace: if a chord key is
+                // delivered right after the release (Karabiner reordering), it
+                // cancels in time.
+                let deadline = DispatchTime.now() + stopGrace
+                DispatchQueue.main.asyncAfter(deadline: deadline) { [weak self] in
+                    guard let self = self else { return }
+                    if self.stopPending && !self.pendingStopCancelled {
+                        self.stopPending = false
+                        self.onSinglePressDown?()
+                    }
+                }
+            }
+        }
+    }
+
+    // MARK: - Idle: start on two taps inside the window
+
+    private func handleDoublePressEvent(_ event: NSEvent) {
+        guard event.type == .flagsChanged, event.keyCode == keyCode else {
+            // A chord key or stray key while a start is pending cancels it
+            // (e.g. ctrl tap + ctrl+C right after the second tap).
+            if startPending && (event.type == .keyDown || event.type == .flagsChanged) {
+                startPending = false
+                pendingStartCancelled = true
+            }
+            return
+        }
+
+        let now = ProcessInfo.processInfo.systemUptime
+        let isDown = !modifierPressed
+
+        if isDown {
+            modifierPressed = true
+
+            if startPending {
+                // Third tap of a buzz while a start is pending: ignore it and
+                // let the pending start fire on its grace timer.
+                return
+            }
+
+            // Evaluate against the PREVIOUS tap before recording this one.
+            let gapFromUp = lastUpAt > 0 ? now - lastUpAt : .infinity
+            let sawRelease = lastUpAt > lastDownAt  // the key was fully released after the previous press
+
+            let qualifies =
+                (sawRelease && gapFromUp <= dpGap) ||   // normal double tap (window from release)
+                (lastDownAt > 0 &&                      // up was merged/dropped by the OS:
+                 now - lastDownAt <= dpNoUpFallback)    // second down came ~immediately after the first
+
+            let afterQuiet = now - modeChangedAt >= quietGap
+            lastDownAt = now
+
+            if qualifies && afterQuiet {
+                startPending = true
+                pendingStartCancelled = false
+                // Chord-confirm grace: ctrl+C pressed right after the second
+                // tap cancels this before recording starts.
+                let deadline = DispatchTime.now() + stopGrace
+                DispatchQueue.main.asyncAfter(deadline: deadline) { [weak self] in
+                    guard let self = self else { return }
+                    if self.startPending && !self.pendingStartCancelled {
+                        self.startPending = false
+                        self.onDoublePress?()
+                    }
+                }
+            }
+        } else {
+            modifierPressed = false
+            lastUpAt = now
+        }
+    }
+
     private func isModifierOnlyKey(_ code: UInt16) -> Bool {
         return [54, 55, 56, 58, 59, 60, 61, 62, 63].contains(code)
     }
